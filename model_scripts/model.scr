import groovy.sql.Sql

def test(Object a){
    def url = 'jdbc:mysql://localhost/alumnos_exa'
    def user = 'root'
    def password = 'root'
    // Extraer tipo en un ENUM
    def driver = 'com.mysql.cj.jdbc.Driver'
    def sql = Sql.newInstance(url, user, password, driver)
    sql.execute '''
      CREATE TABLE Test (
        test   VARCHAR(64)
      );
    '''
        System.out.println("A")
    sql.close()
}